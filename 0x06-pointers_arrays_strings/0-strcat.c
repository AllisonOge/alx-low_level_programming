#include "main.h"
#include <string.h>

/**
 * _strcat - concatenate two strings
 * @dest: destination
 * @src: source
 *
 * Return: pointer to result of src appended to dest
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
